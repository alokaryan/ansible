---
 - host: all
   user: root
   tasks:
           - name: create a user
             user:
                     name: alok
                     status: present

           - name: create another user
             user:
                name: aryan
                status: present

